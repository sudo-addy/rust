# What is the funcion in the rust
- function is a reusable block of code that performs a specific task. In Rust, functions are first-class citizens, meaning:
You can pass them as arguments.
You can return them from other functions.
You can assign them to variables.

fn function_name(parameters) -> return_type {
    // function body
}